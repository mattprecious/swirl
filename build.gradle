allprojects {
  buildscript {
    repositories {
      google()
      mavenCentral()
      jcenter()
    }
  }
  dependencies {
    repositories {
      google()
      mavenCentral()
      jcenter()
    }
  }

  group = GROUP
  version = VERSION_NAME

  apply plugin: 'maven'
}
